[package]
name = "gaze-assembly"
version = "0.9.0-rc.1"
edition = "2021"
rust-version = "1.89"
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Policy-to-pipeline assembly for Gaze"
readme = "README.md"
keywords = ["pii", "gaze", "pipeline"]
categories = ["text-processing"]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
gaze = { path = "../gaze", version = "0.9.0-rc.1", package = "gaze-pii" }
gaze-recognizers = { path = "../gaze-recognizers", version = "0.9.0-rc.1" }
regex = "1"
serde_json = "1"
thiserror = "1"
tracing = "0.1"
