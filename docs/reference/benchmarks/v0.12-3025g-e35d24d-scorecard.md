# #3025 slice G scorecard: shared gov-ID connector (`e35d24d` vs `56e1a3d`)

This report measures the shared cue→value connector grammar for the five SSN /
government-ID recognizers (todo #3025 slice G) at commit `e35d24d` against
`main` at `56e1a3d` — the merge of slice U (#449). G is the second #3025 slice
and rebases onto U, so U's structured-containment rung is in the base. It is
evidence only: no accepted baseline was installed or modified, and
`--accept-baseline` was not used.

## Headline

**On the shipped default (`full-stack-kiji-resolve`) leaked labelled bytes fall
30,461 → 28,659 (−1,802, leak rate 23.3808% → 21.9970%), fully covered entities
10,102 → 10,291 (+189), zero-leak documents 1,126 → 1,143 (+17). The five target
labels account for all of it: SSN −526, DRIVERLICENSENUM −582, IDCARDNUM −384,
NATIONALID −205, TAXNUM −95.** On the deterministic rule floor the same 190
entities move to fully covered for **zero false-positive movement** (rule and
pass2 false-positive bytes both unchanged), and the A4 negative corpus does not
move in any category or cell. Two-run determinism on the candidate is exact.

**Re-score note (the disjointness was measured, not assumed).** G's provisional
scorecard measured against `8d87468` (before U) gave the identical −1,802 and
the identical five per-label reductions. Re-scoring against the U merge
reproduces every one of those numbers byte-for-byte — G (gov-ID labels) and U
(URL / security-token) touch disjoint gold, so the primary axis is unchanged.
The one place the two changes couple is the Kiji safety net: U shortened URL
spans, which shifts clean text, and the `kiji-distilbert-ort` pass re-segments
neighbouring suspects a little differently. That coupling is visible only in the
Kiji false-positive **noise band** — it was +5 FP bytes against `8d87468` and is
−43 FP bytes here — and in the four re-segmented spans below. It is noise, in
the favourable direction, not signal; the gov-ID reductions and the worsened-span
set are identical across both bases.

## Basis and method

| Parameter | Value |
| --- | --- |
| BASE | `56e1a3d` (`main`, slice U merge #449). Its runtime tree is byte-identical to `bfcf264` (U's fix commit): `git diff bfcf264 56e1a3d -- crates scripts Cargo.toml Cargo.lock rust-toolchain.toml` is empty (U's #449 scorecard commit added only docs). The committed [`v0.12-3025u-bfcf264-candidate-scorecard-v4.json`](v0.12-3025u-bfcf264-candidate-scorecard-v4.json) (`227d8100…`, revision `bfcf264`, `dirty = false`) is therefore the BASE scorecard for the merged tree, and no base run was repeated |
| CANDIDATE | `e35d24d` (this branch: G rebased onto `56e1a3d`, `dirty = false` on both runs) |
| Command | `uv run --project scripts/bench python scripts/bench/run_no_opf_benchmark.py full --seed 20260710 --no-download` (run 2 with `--skip-build` against the binary run 1 built) |
| Dataset | `DataikuNLP/kiji-pii-training-data+gaze` at `0275550f0b1f1b8f2dc9356fd31ac1c788b8228b+a4-negative-v1`; composite integrity SHA-256 `11614c80f6d0fe78feb4c592fc9674efac08d73fe5549ad1bed8dd057b7592d2` — identical to every committed v0.12 scorecard |
| Population | 2,910 documents (1,886 Dataiku + 1,024 A4 negatives), 14,719 gold entities, 130,282 labelled PII UTF-8 bytes; every cell completed 2,910/2,910 with an empty failed-closed population on both runs |
| Model bundles | `davlan-mbert-ner-hrl-onnx` `7b0b9d0d…` (7 verified artifacts); `kiji-distilbert` `c129e135…` (3 verified artifacts) — same pins as the base |
| Toolchain | Rust 1.96.0 with `RUSTUP_TOOLCHAIN`, `RUSTC` and `RUSTDOC` exported; worktree-local `target/` (no shared `CARGO_TARGET_DIR`) |
| Hardware | Apple M5 Max (18 cores), 64 GiB, macOS 26.5 |
| Serialization | One anvil worktree, candidate run 1 then run 2 back to back, machine lock (Solo scratchpad 6185) held throughout, no foreign Cargo/Rust process at any point (probed before each run); one-minute load 4.8 at start, 3.8 between runs, 3.6 at end |
| Harness | Byte-identical to the base: `git diff 56e1a3d e35d24d -- scripts/bench crates/gaze-recognizers/examples crates/xtask/fixtures/negative_corpus` is empty |

| Artifact | SHA-256 |
| --- | --- |
| CANDIDATE scorecard, run 1 (`e35d24d`) | `41aa439b1a2d2f037741…` (`v0.12-3025g-e35d24d-candidate-scorecard-v4.json`) |
| CANDIDATE scorecard, run 2 (`e35d24d`) | `b4ddf671da615862ac53…` (`v0.12-3025g-e35d24d-candidate-run2-scorecard-v4.json`) |

Both are committed beside this report. Neither is an accepted baseline.

## Cell results

| Cell | Leaked bytes BASE → CANDIDATE | FP bytes BASE → CANDIDATE | Documents with FP | Fully covered entities | Zero-leak documents | Completion |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `rule-floor-extended` | 99,155 → 97,359 (**−1,796**) | 5,411 → 5,411 (**0**) | 226 → 226 | 1,894 → 2,084 (**+190**) | 1,024 → 1,024 | 2,910/2,910 |
| `pass2-ner` | 32,305 → 30,509 (**−1,796**) | 28,018 → 28,018 (**0**) | 1,944 → 1,944 | 10,000 → 10,190 (**+190**) | 1,122 → 1,138 (**+16**) | 2,910/2,910 |
| `full-stack-kiji-resolve` | 30,461 → 28,659 (**−1,802**) | 168,350 → 168,307 (**−43**) | 2,510 → 2,510 | 10,102 → 10,291 (**+189**) | 1,126 → 1,143 (**+17**) | 2,910/2,910 |

Kiji partition and restore, BASE → CANDIDATE: manifest spans 37,299 → 37,456
(+157), redact actions 1,610 → 1,605 (−5), documents with a redact 633 → 629
(−4), post-policy residual suspects 313 → 312 (−1), initial class mismatches
178 → 178, exact restores 2,277 → 2,281 (+4), restore decision failures 5 → 5,
`strict_would_reject` 1,870 → 1,869, manifest integrity errors 0 → 0. Every
correctness movement is favourable or flat.

`compare_scorecards(candidate, base)` returns `regression.passed = false` with
exactly three failures, all `class_coverage_loss` on `full-stack-kiji-resolve`:
`DATEOFBIRTH` (leaked +3), `SURNAME` (+4), `ZIP` (+3) — the four re-segmented
Kiji spans below. The false-positive ratchets do not fire in any cell (rule and
pass2 unchanged; Kiji improves by 43 FP bytes), and no A4 category moves.
`release_readiness.passed = false` on both halves with the same ten zero-target
gate names as the base (exit code 4, the normal characterisation outcome).

## Per-label movers (Kiji cell)

Format: fully covered / overlapped / leaked UTF-8 bytes, BASE → CANDIDATE.

| Label | Fully covered | Overlapped | Leaked bytes |
| --- | ---: | ---: | ---: |
| `DRIVERLICENSENUM` | 72 → 133 | 78 → 136 | 1,455 → 873 (**−582**) |
| `SSN` | 126 → 174 | 126 → 174 | 982 → 456 (**−526**) |
| `IDCARDNUM` | 50 → 91 | 62 → 103 | 1,456 → 1,072 (**−384**) |
| `NATIONALID` | 52 → 72 | 53 → 73 | 1,788 → 1,583 (**−205**) |
| `TAXNUM` | 35 → 44 | 51 → 60 | 1,988 → 1,893 (**−95**) |
| `BUILDINGNUM` | 39 → 49 | 43 → 53 | 3,098 → 3,080 (−18) |
| `IBAN` | 142 → 142 | 176 → 177 | 1,435 → 1,423 (−12) |
| `PASSWORD` | 4 → 4 | 84 → 85 | 1,825 → 1,819 (−6) |
| `CREDITCARDNUMBER` | 26 → 27 | 31 → 32 | 1,444 → 1,440 (−4) |
| `DATEOFBIRTH` | 0 → 0 | 11 → 10 | 2,874 → 2,877 (**+3**) |
| `SURNAME` | 1,689 → 1,688 | 1,693 → 1,692 | 24 → 28 (**+4**) |
| `ZIP` | 338 → 337 | 358 → 358 | 3,673 → 3,676 (**+3**) |

The five target labels are the grammar-only connector reaching multi-word
phrasing. `BUILDINGNUM`/`IBAN`/`PASSWORD`/`CREDITCARDNUMBER` (−40 bytes together)
are incidental: a gov-ID token now covering a byte that a neighbouring span had
been leaking. The three positive rows are the re-segmentation residual.

## The changed arbitration outcomes, span by span

Both trees were driven through `clean_for_bench` with the harness's own loaders
and every gold span recorded with its overlapping `final_protection_trace`
items (the diagnostic instrument reproduces every committed per-label row of both
trees exactly). Comparing the U-merge base dump against the candidate:

| Cell | Spans improved | Spans worsened | What the improved spans are |
| --- | ---: | ---: | --- |
| `rule-floor-extended` | 190 (1,818 b) | 0 | previously-uncovered gov-ID value → `custom:driver_license` / `custom:ssn` / `custom:national_id` / `custom:tax_number` |
| `pass2-ner` | 190 (1,818 b) | 0 | the same 190 |
| `full-stack-kiji-resolve` | 198 (1,835 b) | 4 (11 b) | the same 190 plus eight cross-effect spans (a gov-ID token covering a byte a neighbour leaked) |

Every improved span on the deterministic floor is a value the single-keyword
connector could not reach and the shared connector now does. There is not one
worsened span and not one new false positive on either deterministic cell — the
connector change is pure recall.

## Residuals (the four Kiji worsened spans, individually)

All four are `kiji-distilbert-ort` safety-net name-suspects that re-segmented
when upstream clean text shifted (the known #2491 sensitivity); none is a shape
the connector rules matched:

1. **`ZIP` `T2E 4L4`** — base had one `kiji-distilbert-ort` name-suspect
   partially covering it; on the candidate that suspect is gone (2 bytes lost).
2. **`ZIP` `CB2 1DP`** — base covered it with two name-suspects, candidate with
   three differently-placed ones (2 bytes lost across the pair).
3. **`DATEOFBIRTH` `15. Mai 2023`** — base had one name-suspect over it,
   candidate none (3 bytes).
4. **`SURNAME` `Herr`** — base had one name-suspect, candidate none (4 bytes).

Each is a document where a nearby gov-ID number changed from raw to a token,
shifting the clean-text byte stream the Kiji pass scans; the safety net's
sub-word split of an unrelated neighbour moved by a few bytes. The
primary-pipeline trace items for these spans are unchanged, and none of the four
is a gov-ID shape.

## Determinism and gates

Run 2 (`--skip-build`, same binary, `dirty = false`) reproduces run 1 exactly:
the complete `_run_correctness_counts` integer set is equal in every cell and
`compare_scorecards(run 2, run 1)` returns `regression.passed = true` with zero
failures. Wall clock per cell, run 1 / run 2: rule 7 s / 6 s, pass2 168 s /
165 s, Kiji 461 s / 467 s.

Gates re-run on the rebased tree at `e35d24d` (U's resolver change compiled in)
under the pinned toolchain, worktree-local `target/`, exit codes: `cargo fmt
--all -- --check` 0, `cargo clippy
--workspace --all-features --all-targets -- -D warnings` 0, `cargo test
--workspace --all-features` 0 (134 test binaries), `xtask fixture-citation-lint`
0, `bundle-tokenization-drift` 0, `locale-cue-bundle-coherence` 0,
`recognizer-composition-validator` 0, `cargo doc -D warnings` 0.

Mutation probe: reverting only `tax_number.cue_anchored`'s connector back to the
single-keyword form turns the drift guard
(`shared_connector_grammar_is_byte_identical_across_the_family`) and that
recognizer's multi-word-connector fixture red while the other four family
recognizers stay green (20 passed / 3 failed); restoring gives all green.
