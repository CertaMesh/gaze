# v0.8 Kiji Benchmark

## TL;DR

- Scope is the current measurable subset only: 5 direct Kiji labels (`Email`, `IBAN`, `credit_card`, `phone`, `postal_code`) plus 2 partial proxies (`first_name`, `surname`) represented by the corpus `Name` span. This is not a 26-label benchmark.
- Headline metric is strict span leak rate: `1 - strict_recall`, where strict means byte-boundary exact match and canonical class exact match. F1 is intentionally not the ranker for this benchmark.
- Gaze rule-floor snapshot on commit `7348690` over 150 fixtures: `Email`, `credit_card`, German `phone`, global `phone`, and German/US `postal_code` have `0.000` strict leak rate; US `phone`, global `postal_code`, and `Name` proxy spans have `1.000` strict leak rate.
- `IBAN` is a scorer-label-map caveat in this snapshot: Gaze emits an equivalent bank-account/IBAN detection, but the current coverage oracle labels it `custom:iban`, so strict canonical class scoring records class mismatches and reports `1.000` strict leak rate until the benchmark label map is made explicit.
- Kiji direct-detector and observer-residual numbers are not populated in this document because this worktree does not contain a pinned Kiji model artifact or frozen benchmark snapshot. The reproducibility contract below requires the model SHA256 before any Kiji numeric claim is citable.

## Methodology

Source methodology: Solo scratchpad 18, "Research synthesis - Kiji SafetyNet evaluation methodology".

The benchmark has two modes:

| Mode | Name | Input | Score meaning |
|---|---|---|---|
| 1 | Direct-detector | Raw coverage-loop fixture text passed directly to Kiji DistilBERT | Measures Kiji as a detector against the committed oracle |
| 2 | Observer-residual | Gaze rule pipeline first, then Kiji as Pass-3 SafetyNet observer over cleaned output | Measures residual leaks the rule floor missed and Kiji can still alert on |

Strict span recall counts a gold span as caught only when both byte boundaries and canonical class match exactly. Partial overlap is a miss in the headline metric and belongs in diagnostics. Class mismatch is also a miss because the manifest contract is typed.

F1 is rejected as the headline ranker. Gaze's Axis 1 asks "what leaked?", so false negatives dominate the safety claim. F1 can hide a high-leak class behind precision or easy classes and is therefore allowed only as a secondary convenience statistic.

The current coverage-loop corpus is synthetic and deterministic. The evaluated rule-floor command was:

```bash
cargo test -p gaze-recognizers --test coverage_loop -- --ignored --nocapture
```

## Reproducibility Contract

| Pin | Value |
|---|---|
| Corpus commit | `7348690` (`origin/main`, includes post-PR #205 150-scenario corpus) |
| Corpus seed | `COVERAGE_CORPUS_SEED=113668362958386` |
| Corpus SHA256 | `c6e78cca59df550fad18e59e9877da03da82c73b80c2368e5233d76353ccfa2f` |
| Fixture count | `150` |
| Gaze tag | `v0.8.0-rc.1-4-g7348690` |
| Rulepack versions | `core=0.5.1`, `core-extended=0.5.1` |
| Kiji model SHA256 | Required before publishing Kiji numbers; not present in this worktree |
| Kiji tokenizer/config SHA256 | Required before publishing Kiji numbers; not present in this worktree |
| Live network fetches | Forbidden in CI; use a local pinned artifact only |

The benchmark result JSON should include these pins plus the scorer version, label-map version, locale set, operating-point parameters, OS/arch/thread metadata, and deterministic output hash. Comparisons against a prior baseline must refuse to compare when the provenance hash changes unless the baseline is explicitly refreshed.

## Results Table

`kiji_strict_recall`, `kiji_strict_leak_rate`, and `observer_residual_count` are marked `not_run` until a pinned Kiji artifact and frozen run are available. `gaze_rule_strict_recall` is from `target/coverage-report.json` generated by the command above.

| Measurable label | Locale | Support | kiji_strict_recall | kiji_strict_leak_rate | gaze_rule_strict_recall | gaze_rule_strict_leak_rate | observer_residual_count |
|---|---:|---:|---:|---:|---:|---:|---:|
| Email | de-DE | 55 | not_run | not_run | 1.000 | 0.000 | not_run |
| Email | en-US | 48 | not_run | not_run | 1.000 | 0.000 | not_run |
| Email | global | 59 | not_run | not_run | 1.000 | 0.000 | not_run |
| IBAN | de-DE | 22 | not_run | not_run | 0.000 | 1.000 | not_run |
| IBAN | en-US | 8 | not_run | not_run | 0.000 | 1.000 | not_run |
| IBAN | global | 18 | not_run | not_run | 0.000 | 1.000 | not_run |
| credit_card | de-DE | 7 | not_run | not_run | 1.000 | 0.000 | not_run |
| credit_card | en-US | 10 | not_run | not_run | 1.000 | 0.000 | not_run |
| credit_card | global | 18 | not_run | not_run | 1.000 | 0.000 | not_run |
| phone | de-DE | 17 | not_run | not_run | 1.000 | 0.000 | not_run |
| phone | en-US | 15 | not_run | not_run | 0.000 | 1.000 | not_run |
| phone | global | 11 | not_run | not_run | 1.000 | 0.000 | not_run |
| postal_code | de-DE | 13 | not_run | not_run | 1.000 | 0.000 | not_run |
| postal_code | en-US | 10 | not_run | not_run | 1.000 | 0.000 | not_run |
| postal_code | global | 6 | not_run | not_run | 0.000 | 1.000 | not_run |
| first_name proxy (`Name`) | de-DE | 29 | not_run | not_run | 0.000 | 1.000 | not_run |
| first_name proxy (`Name`) | en-US | 29 | not_run | not_run | 0.000 | 1.000 | not_run |
| first_name proxy (`Name`) | global | 5 | not_run | not_run | 0.000 | 1.000 | not_run |
| surname proxy (`Name`) | de-DE | 29 | not_run | not_run | 0.000 | 1.000 | not_run |
| surname proxy (`Name`) | en-US | 29 | not_run | not_run | 0.000 | 1.000 | not_run |
| surname proxy (`Name`) | global | 5 | not_run | not_run | 0.000 | 1.000 | not_run |

Do not pool spans across locales. Macro-average within locale first, then across locales, so a high-support global bucket cannot hide a German or US leak cell.

## Composability Summary

Composability metrics are informational and apply after a Kiji run exists:

| Metric | Definition | Status |
|---|---|---|
| `agreement_with_rule_floor` | Kiji span and class exactly agree with a Gaze rule-floor span | `not_run` |
| `expansion_fraction` | Kiji span strictly expands an existing same-class rule-floor span | `not_run` |
| `contradiction_fraction` | Kiji overlaps a rule-floor span but maps to a different canonical class | `not_run` |
| `novel_tp_over_rule_floor` | Kiji catches a strict true positive not emitted by the rule floor | `not_run` |

Observer-residual mode should also report residual alert recall. That number is not an end-to-end masking improvement unless Kiji is moved from observer-only into a blocking path.

## Limitations

- This is not a 26-label Kiji benchmark. The current corpus measures 5 labels directly and 2 labels only through a coarse `Name` proxy; 19 Kiji labels are not measurable in this v0.8 document.
- The corpus has `de-DE`, `en-US`, and `global` buckets only. Locale claims must say German, not DACH.
- The `Name` proxy does not split first name from surname yet, so the two partial proxy labels share the same aggregate span support.
- The current strict scorer needs an explicit benchmark label map for semantically equivalent custom labels, most visibly `custom:iban`.
- No Kiji model artifact is pinned here. Publishing direct-detector or observer-residual Kiji numbers without SHA256 pins would violate the Axis 4 trust contract.
- This synthetic corpus is suitable for regression detection and bounded comparison. It is not a universal anonymization, legal compliance, or production-readiness claim.

## Next Steps

1. Add a local-only `cargo run -p xtask -- kiji-benchmark` wrapper once the pinned Kiji model directory is available. It should emit JSON, never fetch HuggingFace in CI, and refuse to run if `SHA256SUMS` or required artifacts are missing.
2. Freeze `crates/gaze-recognizers/testdata/kiji-benchmark/v0.8-snapshot.json` with Mode 1 direct-detector and Mode 2 observer-residual results.
3. Add the explicit label-map version that resolves corpus labels such as `custom:iban` to the benchmark's canonical classes before treating strict class mismatches as product leaks.
4. Queue the v0.9+ work: full 26-label Kiji benchmark, corpus expansion beyond the 5-direct-plus-2-proxy subset, German locale deepening, and any future OpenMed routing flips.
